{
  "name": "snowrail-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for SnowRail payroll system on Avalanche C-Chain",
  "scripts": {
    "compile": "hardhat compile",
    "clean": "hardhat clean",
    "test": "hardhat test",
    "deploy:fuji": "hardhat run scripts/deploy.js --network fuji",
    "deploy:avalanche": "hardhat run scripts/deploy.js --network avalanche",
    "test:contract": "hardhat run scripts/testContract.js --network avalanche"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^17.2.3",
    "hardhat": "^2.22.0"
  },
  "license": "MIT"
}
